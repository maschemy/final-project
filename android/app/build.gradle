plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    id 'com.google.gms.google-services'
}

android {
    namespace = "com.example.pruge2024"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.pruge2024"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = 23
        targetSdk = 33
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // Firebase BoM (Bill of Materials)을 사용하여 Firebase 버전 관리
    implementation platform('com.google.firebase:firebase-bom:33.5.1')

    // Firebase Analytics 의존성 추가 (필요한 Firebase 서비스 추가)
    implementation 'com.google.firebase:firebase-analytics'

    // 추가적으로 필요한 Firebase SDK를 여기에 추가
    // 예를 들어 Firebase Authentication을 추가하려면:
    implementation 'com.google.firebase:firebase-auth'

    // Firestore 추가:
    // implementation 'com.google.firebase:firebase-firestore'

    // 기타 필요한 Firebase 의존성들을 추가할 수 있습니다.
}